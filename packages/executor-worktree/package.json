{
  "name": "@dahrk/executor-worktree",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "description": "The worktree executor: thin runner adapters (Claude Agent SDK, Codex SDK) behind the Runner interface, a vendored GitService for worktree lifecycle, and the trace producer.",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "spike/live": "tsx spike/run.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.183",
    "@earendil-works/pi-coding-agent": "0.80.6",
    "@openai/codex-sdk": "0.141.0",
    "@dahrk/contracts": "^0.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "tsx": "^4.19.0"
  }
}
